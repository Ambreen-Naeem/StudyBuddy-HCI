# Copy this file to ".env" to override defaults.
# Base URL for the Flask REST API. In dev you can leave this as "/api" and let
# the Vite proxy (vite.config.js) forward to http://localhost:5000.
# For a deployed backend, set the full URL, e.g. https://api.studybuddy.app/api
VITE_API_BASE_URL=/api
